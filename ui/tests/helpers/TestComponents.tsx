import React from 'react';
import { useStore } from '../../src/store/useStore';

// export const TokenWatcher = () => {
export const TokenWatcher = () => {
    // const { accessToken } = useStore();
    return <div data-testid="token-value">DEBUG_TOKEN</div>;
};
